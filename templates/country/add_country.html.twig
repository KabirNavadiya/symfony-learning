{% extends 'base.html.twig' %}

{% block title %}Add Country{% endblock %}

{% block body %}
    <h1>Add New Country</h1>

    {{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.name) }}
    </div>
    <button class="btn btn-success">Save</button>
    <a href="{{ path('app_country_page') }}" class="btn btn-secondary">Back</a>
    {{ form_end(form) }}
{% endblock %}
